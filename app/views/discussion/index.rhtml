<h2><%= "Topics".t %></h2>
<ul>
<% @topics.each do |topic| %>
<li>
	<%= link_to topic.title, :action => "show", :controller => "discussion", :id => topic.id %>
</li>
<% end %>
</ul>

<% if admin? %>
<%= link_to "new topic".t, :action => "new" %>	
<% end %>